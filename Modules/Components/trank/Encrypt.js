/**
 * @fileoverview 这个文件是MD5加密类以及相关方法（API），Encrypt模块 
 * <font color=red>与所有模块没有依赖关系，在BCore.js模块后加载即可</font>
 * @author tenglong.jiang
 * @version 0.1
 */
;(function(window) {
	/**
	 * Encrypt 加密库 
	 * @class
	 * @constructor
	 * @namespace Encrypt命名空间
	 * @constructor
	 * @name Encrypt
	 * @private
	 * @memberOf BCore.encrypt
	 */
	/**
	 *  由于涉及公司业务机密，这里不展示源代码
	 */
})(window);